//AddressUtils.ts

export const fetchAddressForChain = (chainId, tokenType) => {
    //logic  for fetching address
    }
    // Fetch the token address based on the chainId and tokenType
return tokenAddresses[chainId][tokenType];